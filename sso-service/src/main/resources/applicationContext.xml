<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.demo.dao.TblUserDao">

    <resultMap type="com.demo.entity.TblUser" id="TblUserMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="userName" column="userName" jdbcType="VARCHAR"/>
        <result property="passWord" column="password" jdbcType="VARCHAR"/>
        <result property="role" column="role" jdbcType="OTHER"/>
        <result property="isLogin" column="isLogin" jdbcType="OTHER"/>
        <result property="isAdminFirstLogin" column="isAdminFirstLogin" jdbcType="OTHER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="TblUserMap">
        select
          id, userName, password, role, isLogin, isAdminFirstLogin
        from bale.TBL_USER
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="TblUserMap">
        select
          id, userName, password, role, isLogin, isAdminFirstLogin
        from bale.TBL_USER
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="TblUserMap">
        select
        id, userName, password, role, isLogin, isAdminFirstLogin
        from bale.TBL_USER
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="username != null and username != ''">
                and userName = #{username}
            </if>
            <if test="password != null and password != ''">
                and password = #{password}
            </if>
            <if test="role != null">
                and role = #{role}
            </if>
            <if test="islogin != null">
                and isLogin = #{islogin}
            </if>
            <if test="isadminfirstlogin != null">
                and isAdminFirstLogin = #{isadminfirstlogin}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into bale.TBL_USER(userName, password, role, isLogin, isAdminFirstLogin)
        values (#{userName}, #{passWord}, #{role}, #{isLogin}, #{isAdminFirstLogin})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into bale.TBL_USER(userName, password, role, isLogin, isAdminFirstLogin)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.username}, #{entity.password}, #{entity.role}, #{entity.islogin}, #{entity.isadminfirstlogin})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into bale.TBL_USER(userName, password, role, isLogin, isAdminFirstLogin)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.username}, #{entity.password}, #{entity.role}, #{entity.islogin}, #{entity.isadminfirstlogin})
        </foreach>
        on duplicate key update
        userName = values(userName) , password = values(password) , role = values(role) , isLogin = values(isLogin) ,
        isAdminFirstLogin = values(isAdminFirstLogin)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update bale.TBL_USER
        <set>
            <if test="username != null and username != ''">
                userName = #{username},
            </if>
            <if test="password != null and password != ''">
                password = #{password},
            </if>
            <if test="role != null">
                role = #{role},
            </if>
            <if test="islogin != null">
                isLogin = #{islogin},
            </if>
            <if test="isadminfirstlogin != null">
                isAdminFirstLogin = #{isadminfirstlogin},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteByUserName">
        delete from bale.TBL_USER where userName = #{userName}
    </delete>

</mapper>
