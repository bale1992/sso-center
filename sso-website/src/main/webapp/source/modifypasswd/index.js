import React from 'react';
import ReactDOM from 'react-dom';
import ModifyPasswdPage from './ModifyPasswdPage'

ReactDOM.render(
    <React.StrictMode>
        <ModifyPasswdPage />
    </React.StrictMode>,
    document.getElementById('root')
);
